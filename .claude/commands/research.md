---
description: ABSOLUTE ZERO — research a topic into a sourced note.
argument-hint: <topic>
---
Execute the `/research` flow from `FLOW.md` for: $ARGUMENTS

Web search, read sources, summarize into `40_RESEARCH/<topic>.md` with source
URLs + frontmatter, link related topic notes. Report only a 5-line digest to me;
the summary lives in the note, not in context.
